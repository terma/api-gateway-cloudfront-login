{
  "name": "api-gateway-cloudfront-login",
  "version": "0.0.2",
  "description": "Login lambda which to generate CloudFront signed cookies and provide API Gateway AWS IAM login",
  "main": "index.js",
  "scripts": {
    "test": "nyc mocha && nyc check-coverage --lines 95 --functions 95 --branches 85",
    "~test": "mocha --watch",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/terma/api-gateway-cloudfront-login"
  },
  "keywords": [
    "AWS",
    "Lambda",
    "API",
    "Gateway",
    "CloudFront",
    "NodeJS",
    "JavaScript",
    "JS",
    "Login",
    "Assume"
  ],
  "author": "terma",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.167.0",
    "openid-client": "^1.18.2"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "mocha": "^4.0.1",
    "nyc": "^11.3.0",
    "proxyquire": "^1.8.0"
  }
}
